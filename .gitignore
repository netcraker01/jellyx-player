# Rust
# For applications, Cargo.lock SHOULD be committed for reproducible builds.
# It is tracked in this repository; do not add it here.
# In a Cargo workspace, all build artifacts go to the root target/ directory.
target/

# Node
node_modules/
ui/node_modules/
ui/dist/
ui/.svelte-kit/

# Bundled yt-dlp binaries (large, platform-specific — download separately for release)
# These are placed next to the app executable at build time, not committed to git.
helix-desktop/binaries/yt-dlp-*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.atl/

# Build artifacts
*.AppImage
*.deb
*.rpm
*.dmg
*.msi

# Local tooling artifacts
ui/vite.config.ts.timestamp-*.mjs

# SDD process artifacts (internal)
openspec/changes/

# Local promotion and launch planning
docs/promotion/
